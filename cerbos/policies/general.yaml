apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "general"
  rules:
    - actions: ["list", "view"]
      effect: EFFECT_ALLOW
      roles: ["admin", "owner", "manager", "inventory_operator", "sales_representative"]
