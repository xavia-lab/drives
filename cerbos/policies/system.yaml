apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "system"
  rules:
    - actions: ["show", "list"]
      effect: EFFECT_ALLOW
      roles:
        - admin
        - viewer
